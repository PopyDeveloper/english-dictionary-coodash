import {StyleSheet} from 'react-native';

const S = StyleSheet.create({
  container: {},
  title: {
    fontSize: 30,
    fontWeight: '600',
    marginBottom: 10,
  },
  text: {
    fontSize: 16,
    color: '#333',
  },
});

export default S;
