import {StyleSheet} from 'react-native';
import Colors from 'src/contants/Colors';

const S = StyleSheet.create({
  container: {},
  title: {
    fontSize: 30,
    fontWeight: '600',
    marginBottom: 10,
  },
  text: {
    fontSize: 16,
    color: Colors.black,
  },
});

export default S;
