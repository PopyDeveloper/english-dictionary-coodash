import {StyleSheet} from 'react-native';

const S = StyleSheet.create({
  container: {
    width: 30,
    height: 30,
    alignItems: 'center',
    justifyContent: 'center',
    alignSelf: 'flex-end',
  },
  label: {
    fontSize: 18,
    fontWeight: '700',
  },
});

export default S;
